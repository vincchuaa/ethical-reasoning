# Core — required for attack pipeline and defense data generation
openai>=1.0.0
anthropic>=0.30.0
python-dotenv>=1.0.0
tqdm>=4.60.0
jsonlines>=3.0.0

# ERR training only (defense/train.py) — Linux required for deepspeed
transformers>=4.30.0
accelerate>=0.20.0
torch>=2.0.0
scikit-learn>=1.0.0
deepspeed>=0.12.0
wandb>=0.15.0
